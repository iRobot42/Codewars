bool xorf( bool a, bool b ) {
  // 🐲 --my name is Xor!!
  return a ^ b;
  return a - b;
  return a != b;
  return a xor b;
  return a + b & 1;
  return a ? !b : b;
  return ( a + b ) % 2;
  return a & !b || !a & b;
  return ( a | b ) & ( !a | !b );
  return a ? b ? 0 : 1 : b ? 1 : 0;
  // 💃 --I have multilevel protection! 🖕
}
